import { SkeletonWrapper } from './Skeleton.styled';

const Skeleton = () => {
  return (
    <SkeletonWrapper>
      <div className="skeleton" />
    </SkeletonWrapper>
  );
};

export default Skeleton;
